# Documentation index

- `report/` — final Rev 2.1 technical report, dated 16 August 2026, in editable DOCX and PDF form.
- `presentation/` — editable project presentation.
- `firmware-guide/` — historical firmware function guide in DOCX and PDF form; its IP-geolocation, timing, and source-line descriptions predate the recovered final `main.c`.
- `configuration/Blynk_Datastreams.csv` — legacy V0–V12 Blynk export recovered from the project. The canonical V0–V16 definition is `../firmware/BLYNK_DATASTREAMS.md`.
- `../submission/` — later final handoff with Rev A hardware PDFs, XLSX BOM, ERC/difference reports, a second Rev 2.1 document set, the oral-demonstration deck, photographs, and video.

The report is the primary project narrative for architecture, validation scope, repairs, limitations, and bench results. Cross-check exact thresholds, firmware behaviour, schematic values, and manufacturing metadata against the current source and native design files. The later submission's four-layer native stack and inner-plane outputs supersede the earlier two-layer snapshot retained under `hardware/`.
